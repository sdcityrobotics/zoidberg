package exlcm;

struct audio_data_t
{
    int64_t  timestamp;
    int32_t  num_channels;
    int32_t  num_samples;
    float  re_samples[num_samples][num_channels];
    float  im_samples[num_samples][num_channels];
}
